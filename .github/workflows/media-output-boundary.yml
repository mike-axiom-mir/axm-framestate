name: Media output boundary

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install existing media compatibility boundary
        run: python -m pip install Pillow==12.3.0
      - name: Verify media output confinement
        env:
          PYTHONPATH: src
        run: python -m unittest tests.test_media_output_boundary -v
      - name: Verify complete current suite
        env:
          PYTHONPATH: src
        run: python -m unittest discover -s tests -v
